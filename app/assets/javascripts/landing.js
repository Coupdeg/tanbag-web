(function(window, $) {
  'use strict';

  if (!window.Tanbag) {
    window.Tanbag = {};
  }

  var Tanbag = window.Tanbag;

  Tanbag.App = Tanbag.App || {};

  Tanbag.App.landing = {

    initAutoComplete: function() {
			console.log("eiei")
    },

    Init: function() {
      this.initAutoComplete();
    }
  };

})(window, jQuery);